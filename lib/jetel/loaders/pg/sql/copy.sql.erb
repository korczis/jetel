COPY "<%= ctx[:table] %>"
  FROM STDIN
  <% if ctx[:delimiter] || ctx[:row_sep] %>
    WITH DELIMITER '<%= ctx[:delimiter] || ctx[:row_sep] %>'
  <% else %>
    WITH DELIMITER ','
  <% end %>

  CSV HEADER
;

